# 代码库约束
- 本文件针对所有的引擎代码（`namespace SilkEngine.*`），作为编码强制规范；违反 MUST/MUST NOT 视为缺陷，SHOULD 为强烈建议。
- 规则编号由「章节前缀-序号」构成；级别定义：
    - **MUST**：强制，必须遵守
    - **MUST NOT**：禁止
    - **SHOULD**：强烈建议，除非有充分理由且注明

## 一、构造与初始化（C）

- **C-01 [MUST]** 核心架构实例（顶层管理者或任何核心架构成员实例，如 `EngineLoop`/`RenderThreadLoop`/`ThreadManager` 一类）的构造函数只负责数据初始化和初始化逻辑，不负责任何具体过程逻辑，运行流程（帧循环、线程循环等）外置到 `Initialize()`/`Run()`；其余类型开放，构造函数可包含必要的初始化过程逻辑，但须保持清晰与可测试。

- **C-02 [MUST]** 如果没有 `[Service(int priority)]` 标识，则需要在构造函数内完成实例的注册（`Services.Register(this)`）。

- **C-03 [MUST]** 需要自动注册的服务类标注 `[Service(priority, name)]`；Priority 负值区段为基础设施（最先注册 → Shutdown 反序最后释放）；注册顺序按 Priority 升序、类名升序。

- **C-04 [MUST]** 需要两步初始化的类型采用「ctor 数据准备 + `Initialize()/Run()等` 启动」分离模式；`Initialize/Run` 内只做绑定/启动，不重复数据初始化。

- **C-05 [MUST]** 长期依赖必须构造函数注入，禁止在方法内 `new` 依赖实例。

- **C-06 [MUST]** `Dispose` 幂等（`_disposed` 标志守卫）；不接管非自有资源生命周期（线程执行者的 Stop/Join 唯一属主为 `ThreadManager.Shutdown`）。

- **C-07 [SHOULD]** 时间源、超时等可变参数提供 internal 注入点（ctor 参数或 internal 可写属性），供测试缩短/替换，不污染公共 API。

## 二、模块接口通讯（I）

- **I-01 [MUST]** 最顶层接口必须是通用接口，一般只负责数据传输或只读共享，对外暴露数据；模块实际控制通过继承基础接口来实现。
    > 示例:
```CSharp
public interface IInterface1{
    (public) int Id;
    (public) void GetInfo();
}
public interface IInterface1Controller : IInterface1{
    (public) void Start();
    (public) void Stop();
}
```

- **I-02 [MUST]** 跨域（Scene/Render/Assets/Threading/Input）通讯必须经接口，禁止域间直接依赖具体类型。

- **I-03 [MUST]** 接口成员最小化：一个接口一个职责，只含必要成员。

- **I-04 [MUST]** 扩展能力经接口继承追加，禁止修改既有接口成员破坏实现方；除非追加成本远大于修改现有，此时请对用户进行询问。

- **I-05 [SHOULD]** 接口与抽象定义置于独立的 `Abstraction/` 子目录，按子系统分组；实现放对应平台/策略目录。

- **I-06 [MUST]** 接口命名：`I` 前缀；数据/元数据接口用名词性后缀（`Context`/`View`/`State`/`Data`），控制接口用动词性/角色后缀（`Executor`/`Provider`/`Backend`/`Pipeline`/`Importer`/`Decoder`/`Writer`）。

## 三、服务注册与依赖（S）

- **S-01 [MUST]** `Services` 服务定位器仅引擎内部可见（`internal static`）；跨程序集调用方经 `EngineLoop` 公开属性取用，禁止直接访问 `Services`。
- **S-02 [MUST]** 服务注册唯一：重复注册同一类型抛 `InvalidOperationException`；未注册 `Get<T>` fail-fast 抛异常。
- **S-02b [MUST]** `Get<T>` 优先级高于 `TryGet<T>`：凡服务缺失会影响实际运行正确性的场景必须用 `Get<T>`（fail-fast）；`TryGet<T>` 仅用于可选/非关键依赖，获取失败必须显式跳过依赖该服务的功能路径（return/降级），禁止静默继续执行——防止"无异常也无功能"导致无法 DEBUG。
- **S-03 [MUST]** `Services.Shutdown` 反序 Dispose 全部已注册 IDisposable 服务并清空注册表，幂等；关闭后 `Get` 抛未注册异常。
- **S-04 [SHOULD]** 测试夹具经 `Unregister<T>` 清理注册，防跨测试污染；注销不主动 Dispose，由夹具负责。

## 四、线程与并发（T）

- **T-01 [MUST NOT]** 禁止直接 `new Thread()`；线程必须经 `ThreadFactory.CreateThread` 统一创建（命名/后台/优先级一次配置）。
- **T-02 [MUST]** 线程执行者单属主：创建后的 Stop/Join 唯一属主为 `ThreadManager.Shutdown`（反序 Stop → Join），外部模块不得自行停用执行者。
- **T-03 [MUST]** 主线程经 `ThreadManager.RegisterMainThread` 登记；仅主线程可调用的 API 必须 `AssertMainThread()` 亲和断言，非主线程调用抛异常。
- **T-04 [MUST]** 渲染线程只保留渲染职责（后端生命周期、帧同步握手、Passes 执行）；线程控制权归执行者（`ILoopExecutor`），渲染器不创建/持有/释放线程。
- **T-05 [MUST]** 跨线程共享数据必须使用并发容器（`ConcurrentQueue`/`ConcurrentDictionary`）或显式同步，禁止无锁共享可变状态。
- **T-06 [MUST]** 帧末提交语义：销毁/注册/快照 swap 统一由帧末提交阶段（CommitPending）应用，运行期变更不得即时生效破坏帧快照一致性。
- **T-07 [SHOULD]** 阻塞握手必须设置超时，禁止无限挂起。

## 五、场景与生命周期（L）

- **L-01 [MUST]** 组件活跃状态机以 `RecomputeActiveState` 为单一真理源（`Enabled ∧ GameObject.IsActiveInHierarchy`），状态翻转沿（仅翻转沿）幂等派发 `OnEnable`/`OnDisable`；禁止在其他路径直接派发生命周期回调。
- **L-02 [MUST]** 销毁时序：`OnDisable` 先于 `OnDestroy`；`OnDestroy` 帧末销毁队列统一执行且恰一次。
- **L-03 [MUST]** 销毁幂等（`_destroyPending`/`_destroyed` 双标志）；场景切换注销旧场景全部组件。
- **L-04 [MUST]** 组件工厂初始化顺序固定：挂载（AddComponent）→ `ReadFrom`（序列化数据，无则跳过）→ `OnAwake` → `RecomputeActiveState`（Enable）→ 注册。
- **L-05 [MUST]** 生命周期派发经注册表基类索引（按具体类型归类）直读，禁止 `IsSubclassOf` 运行时类型扫描；`Started` 标志保证 `OnStart` 首帧补发仅一次。

## 六、序列化（SR）

- **SR-01 [MUST]** 组件序列化零反射：`Component` 子类经源生成器生成 partial `override WriteTo/ReadFrom`，禁止手写反射序列化。
- **SR-02 [MUST]** `[SerializableInternal]` 仅限引擎程序集使用（引擎外编译错误）；字段排除用 `[NoSerializeField]`；默认规则覆盖全字段、白名单原生 get/set、资产属性感知 GUID、同程序集类型递归展开平面键、外部类型 STJ 兜底。
- **SR-03 [MUST]** 资产引用经 `AssetRefCodec` 字段↔属性配对序列化为 GUID；反序列化经 `AssetManager.TryResolve<T>(Guid)` 解析。
- **SR-04 [MUST]** 组件类型经 `ComponentTypeRegistry.Register<T>()` 泛型登记后反序列化，禁止反射建类型。

## 七、资产（A）

- **A-01 [MUST]** 所有可缓存资产类型实现 `IAsset`（含 `Name`），经 `AssetManager` 统一加载/缓存/引用计数。
- **A-02 [MUST]** 资产 GUID = 路径 MD5（归一化：反斜杠→斜杠、统一小写），纯函数保持静态，跨运行与平台确定性；禁止随机/易变 GUID。
- **A-03 [MUST]** 引用计数自动化闭环：`SetTracked` 赋值计数、`OnDestroy` 级联减计数、帧末 Unloaded 迁移、GPU 释放由消费方在渲染线程帧首执行。
- **A-04 [MUST]** 同步/异步语义分离：缓存条目 Loading 中调用同步 `Load<T>` 抛异常；同一 GUID 异步加载合并等待者，不重复调度；`TryResolve<T>(Guid)` GUID 直查。
- **A-05 [MUST]** 导入层经接口 + 工厂注册（`ImporterFactory`/`IAssetImporter`/`IImageDecoder`，OCP），禁止在管理器内硬编码导入类型。

## 八、渲染（R）

- **R-01 [MUST]** 相机矩阵经绘制命令（`ViewMatrix`/`ProjectionMatrix`）携带，后端按 uniform 上传；禁止突变 `Material`/共享资源以携带每帧数据。
- **R-02 [MUST]** 渲染分层固定：收集（Collector）→ 管线（`IRenderPipeline` 策略）→ Passes（`RenderPass[]`）→ 后端（`IRenderBackend` 执行+呈现）；主线程按 SortOrder 排序，渲染线程直读零排序分配。
- **R-03 [MUST]** 渲染线程帧首处理资产释放队列（UnloadQueue → 后端 `ReleaseGpuResource`），GPU 资源只在渲染线程上下文释放。

## 九、命名与代码风格（N）

- **N-01 [MUST]** 根命名空间尽量少的放置类型，尽量归于子命名空间——`SilkEngine.Core`（仅存放核心通用逻辑）/ `SilkEngine.Assets`（独立命名空间，位于根目录 `Assets/`，不包含于 Core）/ `SilkEngine.Scene`（含 `.Scene.Serialization`）/ `.Render` / `.Threading` / `.InputSystem` / `.Math`；Core 仅承载核心通用逻辑（Services、Log、时钟等），资产系统不得归入 Core。
- **N-02 [MUST]** 公共 API 使用 C# 现代语法：init-only 属性、主构造函数、集合表达式。
- **N-03 [MUST]** 公共方法须有功能注释（用途、参数、返回值）；重写方法以及仅被单一外部调用者使用的方法可免。
- **N-04 [MUST]** 默认禁止使用 `dynamic` / `System.Reflection` 绕过类型安全；**例外**：当绕过类型安全的实现代价（性能、复杂度或可行性的损失）大于使用反射/动态调度的代价时，必须先询问用户，获批准后方可使用，并在代码注释中注明理由。
- **N-05 [MUST]** 仅 `unsafe` 明确标注的代码块可含非安全代码；其余所有逻辑必须 safe。
- **N-06 [MUST]** 全局无状态门面用 `static`（`Time`/`Input`/`Log`）；有状态管理者为实例类（`SceneManager`/`AssetManager`），经 `EngineLoop` 属性或 `Services` 取用。
- **N-07 [MUST]** `.scene` 序列化键随命名空间变化，不向后兼容；同一类型多组件序列化不得互相覆盖。

## 十、测试（TEST）

- **TEST-01 [MUST]** TDD 强制：所有业务逻辑代码必须先写测试 → 失败 → 实现 → 通过。
- **TEST-02 [MUST]** 测试文件按模块分目录：`Math/` `Scene/` `Threading/` `Input/` `Render/` `Core/` `Assets/`（Assets 位于 `Assets/`、Serialization 位于 `Scene/Serialization`）+ `SilkEngine.SourceGen.Tests`（快照/诊断）。
- **TEST-03 [SHOULD]** 测试确定性优先：规避并行 flaky（共享日志写入者、零分配断言等既有问题），必要时夹具隔离（`Unregister`/`Dispose` 解绑）。

## 十一、禁用清单（FORBIDDEN）

- **F-01 [MUST NOT]** 直接 `new Thread()`（必须 `ThreadFactory.CreateThread`）
- **F-02 [MUST NOT]** 使用 `dynamic` / `System.Reflection` 绕过类型安全（未经用户批准的例外除外）
- **F-03 [MUST NOT]** 跨域直接依赖具体类型（必须经接口）
- **F-04 [MUST NOT]** 核心上层架构实例构造函数内执行具体过程逻辑（必须外置 `Initialize`/`Run`）；其余类型允许
- **F-05 [MUST NOT]** 手写反射序列化（必须源生成器）
- **F-06 [MUST NOT]** 跨线程无锁共享可变状态（必须并发容器/显式同步）
- **F-07 [MUST NOT]** 外部模块自行 Stop/Join 线程执行者（属主唯一为 `ThreadManager.Shutdown`）
