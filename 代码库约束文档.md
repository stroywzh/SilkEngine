# 代码库约束
- 本文件针对所有的引擎代码（`namespace SilkEngine.*`）

## 构造约束
- 文件必须有明确的初始化逻辑
    > - 针对上层管理器（参考 `EngineLoop`、`RenderThreadLoop`、`ThreadManager`），构造函数只负责数据初始化和初始化逻辑，不负责任何具体过程逻辑。且如果没有 `[Service(int priority)]` 标识，则需要在构造函数内完成实例的注册（`Service.Register(this)`）
    > - 

- 模块之间的通讯使用接口，且需要满足如下条件
    > - 最顶层接口必须是通用接口，一般只负责数据传输，对外暴露数据。而模块实际控制需要通过继承基础接口来实现。
        > - 示例:
```CSharp
public interface IInterface1{
    (public) int Id;
    (public) void GetInfo();
}
public interface IInterface1Controller : IInterface1{
    (public) void Start();
    (public) void Stop();
}
````

