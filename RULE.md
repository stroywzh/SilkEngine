### 文件与代码操作约束

#### 总则
- 所有授权均**仅对当次操作有效**，不得延续至后续任何变更。
- 对**受保护文件**（详见下方列表）的修改，必须获得单次授权，且仅允许**追加新内容或修改本次新增部分**，严禁修改已有内容。
- 如对用户意图有疑问，应直接提问，不得猜测。

##### 受保护的文件列表
以下条目均为受保护的文件（即使尚不存在）。路径格式为：相对于工作区根目录的相对路径。
- RULE.md（即本文件）

#### 一、批量更改审批
涉及**三个及以上**代码文件或与代码紧密相关的文件（如配置文件、资源文件等）的修改，必须事先获得用户明确授权。

#### 二、设计文档版本控制
未经用户要求，严禁将 `spec`、`design`、`plan` 文档提交至 Git。

#### 三、调试文件使用
- 可参考 `debug` 或 `Debug` 目录下的文件，但不得将其代码直接复制到正式脚本中。
- 复用需重写（通用工具方法除外）。

#### 四、代码可维护性与扩展性
1. **重构**：优先考虑扩展性和长期维护，提供接口抽象、解耦、配置分离等建议。
2. **框架重构**：减少调用方负担，统一命名、简化参数、提供默认实现，调用者仅需知道 API。
3. **注释**：公共方法须有功能注释（用途、参数、返回值），重写方法以及**仅被单一外部调用者**使用的方法可免（如果你无法确定，请询问用户）。
4. **设计原则**：组合优于继承，优先接口；允许非 `MonoBehaviour` 继承以解耦生命周期，减轻 `GameObject` 内存负担。

#### 五、工作区访问限制
未经允许，禁止以任何方式查看、查找或更改工作目录外的内容（包括命令行、网络传输等，`git` 相关命令除外）。